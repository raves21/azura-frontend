export default function PostContent() {
  return (
    <div>
      <p className="text-gray-300">
        Let's set an age limit after which you can't run for political office,
        perhaps a number just below 70 would be a good start.
      </p>
    </div>
  );
}
